number = 17
# Pasará al método el valor que se desea concatenar con la cadena.
print("Tengo {:d} años".format(number))

# Cadena que usa un formateador:
print("Sammy has {} balloons.".format(5))
# Se construyo  una cadena con un par de llaves como marcador de posición ""Sammy has {} balloons.""
# Se agrega el metodo .format()y se pasa el valor del número entero 5 a ese método. Esto coloca el valor de 5 en la cadena donde estaban las llaves

# También se puede asignar una variable para que sea igual al valor de una cadena que tiene marcadores de posición del formateador
open_string = "Tengo {}"
edad = "17 años"
print(open_string.format(edad))

# Los formateadores en Python le permiten usar llaves como marcadores de posición para los valores que pasará con el str.format()método.
# Los valores en los marcadores de posicion no siemppre son numeros.